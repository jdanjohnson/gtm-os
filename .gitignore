.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Node / frontend
frontend/node_modules/
frontend/dist/
# `src/gtm_os/server/frontend_dist/` IS committed — it's the built UI that ships
# with the wheel so `pip install gtm-os && gtm-os start` works end-to-end.
# Rebuild via `cd frontend && npm install && npm run build` (the build script
# copies output into this directory).

# Local data
data/*.db
data/*.db-shm
data/*.db-wal
data/*.sqlite*
*.log

# Local config (template lives in gtm-os.config.yaml.example)
gtm-os.config.yaml

# Environment variables
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
